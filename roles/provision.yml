---
- hosts: "{{ target_group | default('local') }}"

  vars_files:
    - ./../vars.yml  

  tasks:
    - name: install packages
      apt: name={{ item }} update_cache=yes state=latest
      with_items:
      - git
      - vim
      - fail2ban
      - python3
      - nginx